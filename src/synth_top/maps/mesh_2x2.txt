# Bring-up mesh: 2x2 routers, 2 MAG ports, 2 clusters, 4 vector cores.
# The east edge is `nul`, which leaves those router ports tied off without
# changing the grid's shape. Column 2's locals are `nul` for the same reason:
# they held the accumulators of the two-port cluster, and a cluster is one node
# now. See mesh_2x4.txt for the shape that spends them instead.
xxx vec vec xxx
mag mat nul nul
mag mat nul nul
xxx vec vec xxx
