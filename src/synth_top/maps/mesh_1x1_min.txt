# The smallest legal mesh: one router, one MAG port, one cluster, one vector
# core. Exists so a two-mesh interlink bench can carry whole MAGs without
# paying for compute it never exercises.
xxx vec xxx
mag mat nul
xxx nul xxx
